const nextConfig = {
  reactStrictMode: false,
  images: {
    domains: [
      "code.s3.yandex.net",
      "www.youtube.com",
      "pdftron.s3.amazonaws.com",
    ],
  },
};

export default nextConfig;
